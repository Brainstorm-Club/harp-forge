# Design System (vendorizzato)

Copia locale del **Brainstorm Club Design System** (self-hosted, WSG-compliant).
Sincronizzato dal repo `design-system` — commit sorgente: **ffe36eb**.

- `tokens.css` — token `--bsc-*` (importare per primo)
- `components.css` — componenti `.bsc-*`
- `assets/brain-mark.png` — maschera del cervello per `.bsc-brandmark`

Per aggiornare: ricopiare `tokens.css`, `components.css` e `assets/brain-mark.png`
dal repo design-system e aggiornare il commit qui sopra. I font sono self-hosted
in `src/assets/fonts/` (non si usa il CDN Google Fonts del quick-start).
